"""PoC demo app for the grounded pathology VLM.

Streamlit shell + inference / overlay helpers. Reuses GroundedVLM,
the FHIR templater, and existing dataset helpers; adds only the glue
needed to drive an interactive demo.
"""
